{
  "name": "region-resonances-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker proxy for Region Resonances — forwards user queries to OpenAI's embeddings API with rate limiting and CORS.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.36.0"
  }
}
